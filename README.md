# VIDHYATHI-PROJECT

Number Theory & Mathematical Functions This README contains simple language explanations for 34 useful mathematical and number-theory functions. All functions are implemented in Python.

📌 How to Use Copy the code from the Python Code Section and run it in any Python environment.

Explanation of All Functions

1.)factorial(n) Calculates n! which means multiplying all numbers from 1 to n.
2.)is_palindrome(n) Checks if a number reads the same forward and backward.
3.)mean_of_digits(n) Finds the average value of the digits of the number.
4.)digital_root(n) Keeps adding digits of a number until a single digit remains.
5.)is_abundant(n) Returns True if the sum of proper divisors of n is greater than n.
6.)is_deficient(n) Returns True if the sum of proper divisors is less than n.
7.)is_harshad(n) Checks if a number is divisible by the sum of its digits.
8.)is_automorphic(n) Checks if n² ends with the digits of n.
9.)is_pronic(n) Returns True if n = k(k+1) for some integer k.
10.)prime_factors(n) Returns all prime factors of n.
11.)count_distinct_prime_factors(n) Counts how many unique prime factors n has.
12.)is_prime_power(n) Checks if n = pᵏ where p is prime and k ≥ 1.
13.)is_mersenne_prime(p) Checks whether 2ᵖ − 1 is prime (p must be prime)
14.)twin_primes(limit) Returns all pairs of primes that differ by 2.
15.)count_divisors(n) Counts how many positive divisors a number has.
16.)aliquot_sum(n) Sum of all proper divisors of n.
17.)are_amicable(a, b) Checks if both numbers are amicable.
18.)multiplicative_persistence(n) Counts how many steps until digit multiplication becomes a single digit.
19.)is_highly_composite(n) Checks if n has more divisors than any smaller number.
20.)mod_exp(base, exponent, modulus) Efficient method to compute (baseᵉˣᵖ) % modulus.
21.)mod_inverse(a, m) Finds x such that (a·x) % m = 1.
22.)crt(remainders, moduli) Solves Chinese Remainder Theorem problems.
23.)is_quadratic_residue(a, p) Checks if x² ≡ a (mod p) has a solution.
24.)order_mod(a, n) Finds smallest k with aᵏ ≡ 1 mod n.
25.)is_fibonacci_prime(n) Checks if a number is both Fibonacci and prime.
26.)lucas_sequence(n) Generates first n Lucas numbers.
27.)is_perfect_power(n) Checks if n = aᵇ where b > 1
28.)collatz_length(n) Number of steps needed for Collatz sequence to reach 1.
29.)polygonal_number(s, n) Returns the n-th s-gonal number.
30.)is_carmichael(n) Checks if n is a Carmichael number.
31.)is_prime_miller_rabin(n, k) Probabilistic prime-testing algorithm.
32.)pollard_rho(n) Fast algorithm for prime factorization.
33.)zeta_approx(s, terms) Approximates Riemann zeta function.
34.)partition_function(n) Number of ways to write n as a sum of positive integers.

   DATASETS - This project does not use external datasets . All functions operate on user-provided numerical inputs only. you can use fibonacci,or lucas number

               TOOLS AND TECHNOLOGIES USED

                     Python-3
                     Math library
                     Randam Library
                     Built-in Functions
